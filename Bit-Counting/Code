# code
'''Python
def count_bits(n):
    l1=list(bin(n))
    l2=[]
    if n<0:
        return "Use only non-negatives!"
    else:
        for i in l1:
            if i=='1':
                l2.append(i)
        return len(l2)
'''

# code
'''Python
def count_bits(n):
    if n<0:
        return "Use only non-negatives!"
    else:
        return bin(n).count("1")
'''
